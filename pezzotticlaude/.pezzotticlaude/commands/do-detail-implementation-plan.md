Please read carefully the implemention plan at $ARGUMENTS and tell me if everything is clear. We need to write a detailed implementation plan so that we can start working on this.

Break down the whole plan at $ARGUMENTS into small, actionable and sequential task. Each task should have a status ([ ] for un-done, [~] for in-progress and [x] for done), a description, context and samples (if applicable). Write down this detailed action plan to a file.
